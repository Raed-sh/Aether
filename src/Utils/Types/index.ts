export type SearchData = {
    address:string
    filter:string
}

export type TxnDetails = {
    txn_hash1:string,
    txn_hash2:string,
    status:string,
    timestamp:string,
    from:string,
    to:string,
    value:string,
    txn_fee:string,
}